{{- include "flow.rejectTrafficModeAnnotation" (dict "annotations" .Values.livekit.signal.publicService.annotations "path" "livekit.signal.publicService.annotations") -}}
{{- include "flow.rejectTrafficModeAnnotation" (dict "annotations" .Values.livekit.rtc.annotations "path" "livekit.rtc.annotations") -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "flow.fullname" . }}-livekit
  labels:
    {{- include "flow.labels" . | nindent 4 }}
    app.kubernetes.io/component: livekit
spec:
  replicas: {{ .Values.livekit.replicaCount }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 0
  selector:
    matchLabels:
      {{- include "flow.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: livekit
  template:
    metadata:
      labels:
        {{- include "flow.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: livekit
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/livekit-config.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "flow.serviceAccountName" . }}
      automountServiceAccountToken: false
      {{- if .Values.runtime.hostNetwork }}
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      topologySpreadConstraints:
        {{- include "flow.topologySpread" (dict "root" . "component" "livekit") | nindent 8 }}
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - topologyKey: kubernetes.io/hostname
              labelSelector:
                matchLabels:
                  {{- include "flow.selectorLabels" . | nindent 18 }}
                  app.kubernetes.io/component: livekit
      terminationGracePeriodSeconds: 18000
      containers:
        - name: livekit
          image: "{{ .Values.livekit.image.repository }}:{{ .Values.livekit.image.tag }}"
          imagePullPolicy: {{ .Values.livekit.image.pullPolicy }}
          args:
            - --config
            - /etc/livekit/livekit.yaml
            - --key-file
            - /etc/livekit-keys/livekit-keys.yaml
            - --bind
            - {{ if .Values.runtime.hostNetwork }}$(FLOW_HOST_IP){{ else }}0.0.0.0{{ end }}
          {{- if .Values.runtime.hostNetwork }}
          env:
            - name: FLOW_HOST_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
          {{- end }}
          ports:
            - name: signal
              containerPort: 7880
              protocol: TCP
            - name: metrics
              containerPort: {{ .Values.livekit.metrics.port }}
              protocol: TCP
            - name: rtc-tcp
              containerPort: {{ .Values.livekit.rtc.tcpPort }}
              {{- if .Values.runtime.hostNetwork }}
              hostPort: {{ .Values.livekit.rtc.tcpPort }}
              {{- end }}
              protocol: TCP
            - name: rtc-udp
              containerPort: {{ .Values.livekit.rtc.udpPort }}
              {{- if .Values.runtime.hostNetwork }}
              hostPort: {{ .Values.livekit.rtc.udpPort }}
              {{- end }}
              protocol: UDP
          readinessProbe:
            httpGet:
              path: /
              port: signal
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /
              port: signal
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 5
          startupProbe:
            httpGet:
              path: /
              port: signal
            periodSeconds: 2
            failureThreshold: 60
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          resources:
            {{- toYaml .Values.livekit.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /etc/livekit
              readOnly: true
            - name: keys
              mountPath: /etc/livekit-keys
              readOnly: true
            - name: turn-secret
              mountPath: /etc/livekit-turn
              readOnly: true
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: config
          {{- if .Values.livekit.existingConfigSecret }}
          secret:
            secretName: {{ .Values.livekit.existingConfigSecret }}
          {{- else }}
          configMap:
            name: {{ include "flow.fullname" . }}-livekit-config
          {{- end }}
        - name: keys
          secret:
            secretName: {{ include "flow.secretName" . }}
            items:
              - key: livekit-keys.yaml
                path: livekit-keys.yaml
            defaultMode: 0440
        - name: turn-secret
          secret:
            secretName: {{ include "flow.secretName" . }}
            items:
              - key: turn-shared-secret
                path: turn-shared-secret
            defaultMode: 0440
        - name: tmp
          emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "flow.fullname" . }}-livekit
  labels:
    {{- include "flow.labels" . | nindent 4 }}
    app.kubernetes.io/component: livekit
spec:
  type: ClusterIP
  selector:
    {{- include "flow.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: livekit
  ports:
    - name: signal
      port: 7880
      targetPort: signal
      protocol: TCP
      appProtocol: http
    - name: metrics
      port: {{ .Values.livekit.metrics.port }}
      targetPort: metrics
      protocol: TCP
      appProtocol: http
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "flow.fullname" . }}-livekit-signal
  labels:
    {{- include "flow.labels" . | nindent 4 }}
    app.kubernetes.io/component: livekit
  annotations:
spec:
  type: ClusterIP
  selector:
    {{- include "flow.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: livekit
  ports:
    - name: signal
      port: {{ .Values.livekit.signal.servicePort }}
      targetPort: signal
      protocol: TCP
      appProtocol: http
{{- if .Values.livekit.signal.publicService.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "flow.fullname" . }}-livekit-signal-public
  labels:
    {{- include "flow.labels" . | nindent 4 }}
    app.kubernetes.io/component: livekit
  annotations:
    networking.heteronetwork.io/traffic-mode: "forwarded"
    {{- with .Values.livekit.signal.publicService.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  type: LoadBalancer
  loadBalancerClass: heteronetwork.io/public
  allocateLoadBalancerNodePorts: false
  externalTrafficPolicy: Cluster
  {{- with .Values.livekit.signal.publicService.loadBalancerSourceRanges }}
  loadBalancerSourceRanges:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    {{- include "flow.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: livekit
  ports:
    - name: signal
      port: {{ .Values.livekit.signal.publicService.port }}
      targetPort: signal
      protocol: TCP
      appProtocol: http
{{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "flow.fullname" . }}-livekit-rtc
  labels:
    {{- include "flow.labels" . | nindent 4 }}
    app.kubernetes.io/component: livekit
  annotations:
    networking.heteronetwork.io/traffic-mode: "forwarded"
    {{- with .Values.livekit.rtc.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  type: LoadBalancer
  loadBalancerClass: heteronetwork.io/public
  allocateLoadBalancerNodePorts: false
  externalTrafficPolicy: Cluster
  selector:
    {{- include "flow.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: livekit
  ports:
    - name: rtc-tcp
      port: {{ .Values.livekit.rtc.tcpPort }}
      targetPort: rtc-tcp
      protocol: TCP
    - name: rtc-udp
      port: {{ .Values.livekit.rtc.udpPort }}
      targetPort: rtc-udp
      protocol: UDP
